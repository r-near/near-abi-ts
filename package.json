{
  "name": "near-abi-ts",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc",
    "start": "bun index.ts",
    "test": "vitest"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@types/bun": "latest",
    "rimraf": "^6.0.1",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
